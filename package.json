{
  "scripts": {
    "test-api": "./node_modules/mocha/bin/mocha ./jsonplaceholder/test --recursive --reporter mochawesome --reporter-options reportDir=reports/mochawesome/ --timeout 180000",
    "reports": "open 'reports/mochawesome/mochawesome.html'"
  },
  "dependencies": {
    "chai": "^4.1.2",
    "dotenv": "^7.0.0",
    "mocha": "^8.4.0",
    "mochawesome": "^3.0.2",
    "supertest": "^3.1.0"
  },
  "devDependencies": {
    "chai-json-schema": "^1.5.1"
  }
}
